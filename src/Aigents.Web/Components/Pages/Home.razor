@page "/"
@page "/buy"
@page "/sell"
@page "/rent"
@page "/agent"
@rendermode @(new InteractiveServerRenderMode(prerender: true))
@using Microsoft.AspNetCore.Components.Web
@inject NavigationManager Navigation

<PageTitle>Aigents — Stop paying realestate.com.au's photo tax. AI-powered listings for Australian agents.</PageTitle>

@* Show Hero - Subdomain Routing handled by Hero component *@
<Hero OnGetStarted="HandleGetStarted" />

@code {
    private void HandleGetStarted(string mode)
    {
        Navigation.NavigateTo($"/chat?mode={mode}", forceLoad: false);
    }
}
